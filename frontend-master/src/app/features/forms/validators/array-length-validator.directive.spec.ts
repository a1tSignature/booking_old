import { ArrayLengthValidatorDirective } from './array-length-validator.directive';

describe('ArrayLengthValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new ArrayLengthValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
